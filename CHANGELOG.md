# Changelog

## 0.0.3（2026-09-19）
- **默认值变更**：`briefScope` 默认从 `any` 改成 `session`。旧口径是"工作目录 `.grill/` 下最新一份
  即当前简报"，实测会出问题：2026-09-18 为某个任务冻结的简报，在 09-19 的无关任务里仍然生效，
  把正常的编辑判成 `out_of_scope`（连续两次）。新口径只认**本轮会话**期间冻结或改过的那一份；
  要"一份简报管整个目录"就显式写 `briefScope: any`。
- 读取处也兜默认值（配置对象可能没走 schema：测试直传、运行时覆盖文件只改个别键），
  避免"schema 默认改了、直传路径没变"。

## 0.0.2（2026-09-19）
- Tier 3 回复护栏：挂在 `agent/turn-stopping`，只判 Lead 面向用户的最终回复（隐喻行话／未解释术语／
  含糊指代／废话填充），命中就回灌重写，每轮上限 `replyMaxRewrites`（默认 1）；子代理与 teammate 豁免。
- 与 `dsh-jev` 闸门合并：把自己的 5/9 条规则挂进闸门的那一次 Jev 调用，一次通过的工具调用
  从两次网络往返降到一次；闸门服务缺席时自动退回独立判定。
- 贡献者注册表改成进程级（热重载后换了服务实例仍看得到注册），并加有效期与 60 秒续期。
- 补 `LICENSE`；README 重写；补 `repository` / `engines` / `files` / `test` 等元数据。

## 0.0.1（2026-09-18）
- 首次实现：Tier 0 确定性路径底线（归档／vendor 源码／权威文件，0 ms，不调模型）
  + Tier 1/2 基于冻结简报的范围判定（5 条 noul 问题，deny 与 ask 分档阈值），
  判定失败一律放行交回宿主权限链，每次判定落 JSONL 流水。
