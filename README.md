# @dsh-external/dsh-jev-warden

Jev warden: enforces agent-constraint skills (grill-me) by judging tool calls against the frozen brief and stopping clear out-of-scope actions.

由 dsh-super-injector dev_scaffold_plugin 生成。

## 构建与注入

```bash
DSH_CHECKOUT=<checkout> bash scripts/build.sh
# 注入器环境内：dev_inject_plugin <本目录>
```
